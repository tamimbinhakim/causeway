# `raises`

Declare typed errors a handler may raise. See [`@raises`](../decorators/raises.md) — they're the same function (`raises` is a decorator).

This page exists so the function index has the symbol; the decorator page has the full docs.

```python
from causeway import raises
```

## See

- [`@raises` (decorator)](../decorators/raises.md)
- [Errors](../../backend/errors.md)
