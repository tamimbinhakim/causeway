# Copy to `.env` for local dev. Never commit `.env`.

# Quay dev server
QUAY_HOST=127.0.0.1
QUAY_PORT=8000

# Reference task adapter (Dramatiq) — local Redis broker
QUAY_REDIS_URL=redis://127.0.0.1:6379/0

# Reference vector / DB
QUAY_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/quay
