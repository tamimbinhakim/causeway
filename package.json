{
  "name": "quay-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "A lean backend framework on top of Dyadpy — file-based routing, typed config/DI, plugin registry, and background tasks for Python.",
  "homepage": "https://github.com/tamimbinhakim/quay#readme",
  "bugs": {
    "url": "https://github.com/tamimbinhakim/quay/issues"
  },
  "license": "MIT",
  "author": "Tamim Bin Hakim (@tamimbinhakim)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tamimbinhakim/quay.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "pnpm lint:py && pnpm lint:docs",
    "lint:py": "uv run --directory packages/quay ruff check .",
    "lint:docs": "prettier --check \"**/*.{md,json,yaml,yml}\"",
    "format": "pnpm format:py && pnpm format:docs",
    "format:py": "uv run --directory packages/quay ruff format .",
    "format:docs": "prettier --write \"**/*.{md,json,yaml,yml}\"",
    "format:check": "pnpm format:check:py && pnpm format:check:docs",
    "format:check:py": "uv run --directory packages/quay ruff format --check .",
    "format:check:docs": "prettier --check \"**/*.{md,json,yaml,yml}\"",
    "typecheck": "uv run --directory packages/quay mypy src",
    "test": "uv run --directory packages/quay pytest",
    "clean": "rimraf node_modules .turbo packages/*/dist packages/*/.venv",
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "21.0.1",
    "@commitlint/config-conventional": "21.0.1",
    "husky": "9.1.7",
    "lint-staged": "17.0.4",
    "prettier": "3.8.3",
    "rimraf": "6.1.3"
  },
  "lint-staged": {
    "packages/quay/**/*.py": [
      "uv run --directory packages/quay ruff check --fix",
      "uv run --directory packages/quay ruff format"
    ],
    "*.{md,json,yaml,yml}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@11.0.9"
}
