[build-system]
requires = ["hatchling>=1.29.0"]
build-backend = "hatchling.build"

[project]
name = "quay-tasks-dramatiq"
version = "0.1.0a0"
description = "Dramatiq adapter for Quay's background-task contract."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "Tamim Bin Hakim" }]
dependencies = [
  "quay>=0.1.0a0",
  "dramatiq[redis]>=2.1.0",
]

[project.entry-points."quay.plugins"]
tasks-dramatiq = "quay_tasks_dramatiq:plugin"

[project.optional-dependencies]
dev = ["pytest>=9.0.3", "pytest-asyncio>=1.3.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/quay_tasks_dramatiq"]
