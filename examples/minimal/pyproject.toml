[project]
name = "causeway-example-minimal"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "causeway>=0.1.0a0",
]

[tool.uv.sources]
causeway = { path = "../../packages/causeway", editable = true }

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "nuitka>=4.1",
]
