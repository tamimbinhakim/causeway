"""App-root providers.

Subtrees may declare their own `_scope.py` files with their own providers and
route-scoped lifecycle hooks; app-level lifespan and plugins are owned by
`create_app`.
"""

from __future__ import annotations
